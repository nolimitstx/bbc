'use client';
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#FB6A14', // Your primary color
    },
    secondary: {
      main: '#ccc', // Your secondary color
    },
  },
});

export default theme;